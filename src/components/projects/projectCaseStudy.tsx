export default function ProjectCaseStudy({ name, description, image, tags, year }: { name: string; description: string; image: string; tags: string[]; year: number }) {
    return (
        <div className="absolute border-2 border-border rounded-md overflow-hidden w-[90%] h-[90%]">
            prueba
        </div>
    )
}